# This file used to create a StdScheduleFactory instance.
# By default, StdSchedulerFactory is the factory that create a Schedule.  

# All of properties are not required.

org.quartz.scheduler.instanceName = QuartzScheduler
org.quartz.threadPool.threadCount = 3
org.quartz.jobStore.class = org.quartz.simpl.RAMJobStore